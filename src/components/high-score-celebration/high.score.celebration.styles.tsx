import styled from "styled-components";

export const HighScoreMessage = styled.div`
  position: absolute;
  top: 0.75rem;
  left: 50%;
  transform: translateX(-50%);
  z-index: 99;
  display: grid;
  gap: 0.35rem;
  text-align: center;
  pointer-events: none;
  color: #fff7a8;
  font-size: 1rem;
`;

export const HighScoredSmashed = styled.div``;
